@model BriefSys.Models.Acceso_Usuario

@{
    ViewBag.Title = "Registrarse";
}

<div>
    <h2>@ViewBag.Title</h2>

    <form action="Register" method="post">
        <div class="form-group">
            @Html.LabelFor(u => u.UsuarioID)
            @Html.TextBoxFor(u => u.UsuarioID, new { @class = "form-control", placeholder = "Usuario" })
        </div>
        <div class="form-group">
            @Html.LabelFor(u => u.Password)
            @Html.TextBoxFor(u => u.Password, new { @class = "form-control", placeholder = "Password" })
        </div>
        <input type="submit" value="Registrarse" />
    </form>

    @*@using (Html.BeginForm("Register", "Acceso", FormMethod.Post))
        {
            <div class="row">
                <div class="form-group">
                    <div>
                        @Html.LabelFor(u => u.UsuarioID)
                        @Html.TextBoxFor(u => u.UsuarioID)
                    </div>
                    <div>
                        @Html.LabelFor(u => u.Password)
                        @Html.TextBoxFor(u => u.Password)
                    </div>
                </div>
                <div class="form-group">
                    <input type="submit" />
                </div>
            </div>
        }*@
</div>